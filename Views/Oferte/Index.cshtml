@model PaginatedList<MagazinElectronice_Pascu_Ioana.Models.Oferta>

@{
    ViewData["Title"] = "Oferte";
}

<h1>Ofertă</h1>

<p>
    <a asp-action="Create">Creaază nou</a>
</p>
<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Caută după denumire: <input type="text" name="stringCautare" value="@ViewData["CurrentFilter"]" />
            <input type="submit" value="Căutare" class="btn btn-default" /> |
            <a asp-action="Index">Înapoi la listă</a>
        </p>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-ordineSortare="@ViewData["DenumireSortParm"]" asp-route-currentFilter="@ViewData["CurrentFilter"]">Denumire</a>
            </th>
            <th>
                Preț vechi
            </th>
            <th>
                <a asp-action="Index" asp-route-ordineSortare="@ViewData["PretRedusSortParm"]" asp-route-currentFilter="@ViewData["CurrentFilter"]">Preț redus</a>
            </th>
            <th>
                Valabilitate
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Denumire)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PretVechi)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PretRedus)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Valabilitate)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Editare</a> |
                    <a asp-action="Details" asp-route-id="@item.ID">Detalii</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID">Ștergere</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@{
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}
<a asp-action="Index"
   asp-route-ordineSortare="@ViewData["CurrentSort"]"
   asp-route-numarPagina="@(Model.PageIndex - 1)"
   asp-route-currentFilter="@ViewData["CurrentFilter"]"
   class="btn btn-default @prevDisabled">
    Înapoi
</a>
<a asp-action="Index"
   asp-route-ordineSortarte="@ViewData["CurrentSort"]"
   asp-route-numarPagina="@(Model.PageIndex + 1)"
   asp-route-currentFilter="@ViewData["CurrentFilter"]"
   class="btn btn-default @nextDisabled">
    Înainte
</a>